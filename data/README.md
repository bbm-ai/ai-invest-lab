# ai-invest-lab

Opinionated skeleton for a 21‑day AI 投資實驗室。這是 *v2* 骨架（輕量、可本機運行）。

## Quickstart
```bash
python3 -m venv .venv && source .venv/bin/activate
pip install -U pip
pip install -r requirements.txt
cp .env.example .env  # 填入金鑰（可先留空）
python scripts/smoke_test.py
```
